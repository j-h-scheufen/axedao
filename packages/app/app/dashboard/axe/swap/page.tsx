import { Swap } from '@/components/axe';

export default function SwapPage() {
  return (
    <section className="mx-5 flex flex-col items-center justify-center gap-4 py-8 md:py-10">
      <Swap />
    </section>
  );
}
