import { MemberBadge } from '@/components/axe/dao';

export default function SwapPage() {
  return (
    <section className="flex flex-col items-center justify-center gap-2 sm:gap-4 width-full">
      <MemberBadge />
    </section>
  );
}
