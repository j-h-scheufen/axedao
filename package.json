{
  "name": "axe-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Axé DAO & Token Project Monorepo",
  "author": "Jan Hendrik Scheufen <j.h.scheufen@gmail.com>",
  "repository": "git@github.com:j-h-scheufen/axedao.git",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "build": "yarn workspaces foreach -Wp run build",
    "test": "yarn workspaces foreach -Wp run test",
    "lint": "yarn workspaces foreach -Wp run lint"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "packageManager": "yarn@4.5.0",
  "dependencies": {
    "file-type": "^19.6.0",
    "sharp": "^0.33.5"
  }
}
